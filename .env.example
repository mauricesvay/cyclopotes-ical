# Example environment variables for cyclopotes-ical
# URL to fetch events from (default used by app is https://cyclopotes.cc/event)
EVENT_URL=https://cyclopotes.cc/event
# Optional POST body to send to the events API (JSON string). If empty, a default body will be sent with
# `start` and `end` set to the current month's first and last day, and default filters.
# Example: EVENT_POST_BODY={"start":"2026-01-01","end":"2026-01-31","types":[1,2,3]}
EVENT_POST_BODY=

# Use the local events.json instead of fetching remotely
# Set to 1 to use local file
USE_LOCAL_EVENTS=0

# Logging
LOG_FILE=./cyclopotes.log
LOG_LEVEL=info

# S3 upload settings
S3_BUCKET=your-bucket-name
S3_KEY=cyclopotes.ics
AWS_REGION=eu-west-1
# The AWS SDK will pick up credentials from env or usual provider chain
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=

# Optional
TZ=Europe/Paris
